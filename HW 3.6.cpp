#include <iostream>
#include <cmath>
using namespace std;



int main() 
{
    cout<<"a=?  b=?  c=? ";
    float a,b,c;
    cin>>a>>b>>c;
    if (abs(a)>=abs(b) and abs(a)>=abs(b)) cout<<"max: "<<a;
    else if (abs(b)>=abs(a) and abs(b)>=abs(c)) cout<<"max: "<<b;
        else cout<<"max: "<<c;
    c
    if (abs(a)<=abs(b) and abs(a)<=abs(b)) cout<<" min: "<<a;
    else if (abs(b)<=abs(a) and abs(b)<=abs(c)) cout<<" min: "<<b;
        else cout<<" min: "<<c;
    
    
}